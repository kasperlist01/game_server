import React from 'react';

const ProfilePage = ({ game }) => {
    return (
        <div>
            <h2>User Profile ({game})</h2>
            <p>Here will be the user profile information...</p>
        </div>
    );
};

export default ProfilePage;
